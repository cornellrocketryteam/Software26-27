# Flight software

This directory will contain the shared Hybrid/Liquid Rust flight-software workspace.

The code will select vehicle-specific behavior through reviewed build-time configuration. Before adding a crate, define its interface, target hardware, test strategy, and the owner responsible for flight-readiness evidence.
