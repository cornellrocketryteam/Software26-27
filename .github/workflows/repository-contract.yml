name: Repository contract

on:
  pull_request:
  push:
    branches: [main]

jobs:
  repository-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sh tests/test_repository_layout.sh
